@PACKAGE_INIT@

get_filename_component(PARENT_DIR ${CMAKE_CURRENT_LIST_DIR} DIRECTORY)
get_filename_component(INSTALLED_LIB_DIR ${PARENT_DIR} DIRECTORY)
set(subprocess.h_INCLUDE_DIRS "${CMAKE_INSTALL_PREFIX}/include")
